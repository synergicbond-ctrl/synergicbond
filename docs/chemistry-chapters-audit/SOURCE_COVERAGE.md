# Source Coverage

## Status: not yet attempted for any chapter

The handover document asks for a full source-coverage matrix (source file → page/section →
destination route → status) built by comparing each chapter's live content against its
original founder-supplied source file (DOCX/PDF/ZIP in `~/Downloads` or `~/Documents`).

That comparison has **not** been done in this branch. The Adsorption and S-Block work so far
was structural (dead branding, duplicate sections, route de-duplication) and did not involve
opening `Adsorption_Complete_Strong_Notes (3).docx` or any other source file to check the
on-site prose against it sentence-by-sentence.

This file is a placeholder acknowledging that gap rather than a completed matrix — filling it
in properly means, per chapter:
1. Locating the exact source file(s) used (per `docs/` conventions and the handover doc's
   §12 "Known source files").
2. Extracting the source text/structure.
3. Walking the live route section-by-section and recording `preserved` / `corrected` /
   `expanded` / `duplicated` / `missing` / `unclear` for each source statement.

None of chapters 1–13 have this matrix populated yet.
