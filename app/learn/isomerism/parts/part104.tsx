import Link from "next/link";
const cases=[[28,"substituted cyclobutane",4,4],[29,"substituted cyclopentane",4,4],[30,"substituted cyclohexane",8,8],[31,"three-arm rigid small-ring system",10,10]] as const;
function Ring({sides,variant}:{sides:number;variant:number}){const points=Array.from({length:sides},(_,i)=>{const a=-Math.PI/2+i*2*Math.PI/sides;return `${100+45*Math.cos(a)},${62+38*Math.sin(a)}`}).join(" ");return <svg viewBox="0 0 200 125" className="w-full" role="img" aria-label={`ring configuration ${variant+1}`}><polygon points={points} fill="none" stroke="currentColor" strokeWidth="2.5"/>{Array.from({length:Math.min(sides,4)},(_,i)=><path key={i} d={`M${100+45*Math.cos(-Math.PI/2+i*2*Math.PI/sides)} ${62+38*Math.sin(-Math.PI/2+i*2*Math.PI/sides)}v${variant%2===i%2?"-21":"21"}`} stroke="#f0abfc" strokeWidth="2.5"/>)}<text x="100" y="120" textAnchor="middle" fill="#86efac" fontWeight="700">configuration {variant+1}</text></svg>}
export default function IsomerismPart104(){return <main className="min-h-screen bg-[#08111f] px-4 py-8 text-white sm:px-6 sm:py-12"><article className="mx-auto max-w-6xl"><Link href="/learn/isomerism" className="rounded-full border border-cyan-300/30 bg-cyan-300/10 px-4 py-2 text-sm font-bold text-cyan-100">Isomerism</Link><header className="mt-6 rounded-3xl border border-cyan-300/15 bg-[#08111f] p-6 sm:p-9"><p className="text-xs font-black uppercase tracking-[.24em] text-cyan-200">Isomerism source · page 94 · Part 104</p><h1 className="mt-3 text-3xl font-black sm:text-5xl">Counting cis/trans configurations in substituted rings</h1><p className="mt-4 text-slate-300">The source lists distinct ring-face arrangements rather than applying 2ⁿ without checking symmetry-equivalent up/down patterns.</p></header><section className="mt-7 space-y-7">{cases.map(([number,title,count,sides])=><section key={number} className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><h2 className="text-2xl font-black text-cyan-100">Source example {number}: {title}</h2><p className="mt-2 text-sm leading-6 text-slate-300">Assign every substituent to the upper or lower ring face, then merge combinations related by the molecular symmetry shown in the source.</p><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{Array.from({length:count},(_,i)=><article key={i} className="rounded-xl border border-cyan-300/20 p-3"><Ring sides={sides} variant={i}/></article>)}</div><p className="mt-4 text-sm text-amber-100"><b>Source count:</b> {count} geometrical isomers.</p></section>)}</section><aside className="mt-7 rounded-2xl border border-amber-300/25 bg-amber-300/[.07] p-5 text-sm leading-6 text-amber-50"><b>Ring-face reminder:</b> a solid or hashed substituent in a perspective drawing records face, not axial/equatorial preference. Keep the up/down relation fixed while comparing configurations.</aside></article></main>}
